/// This enum represents the type of media feature.
enum MediaFeatureType {
  /// The media is an image.
  image,

  /// The media is a video.
  video,

  /// The media is an audio.
  audio,

  /// The media should be treated as a generic file.
  file,
}
